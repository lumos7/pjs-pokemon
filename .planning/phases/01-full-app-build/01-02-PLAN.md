---
phase: 01-full-app-build
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scenes.ts
  - lib/voices.ts
  - lib/pokemon.ts
autonomous: true
requirements:
  - REQ-003

must_haves:
  truths:
    - "lib/scenes.ts exports scene config for canyon, coast, forest, glacier"
    - "lib/voices.ts exports voice IDs array and pickRandomVoice function"
    - "lib/pokemon.ts exports fetchFirst250 and getOfficialArtworkUrl"
  artifacts:
    - path: "lib/scenes.ts"
      provides: "Scene configuration"
      exports: ["scenes", "Scene"]
    - path: "lib/voices.ts"
      provides: "ElevenLabs voice IDs and picker"
      exports: ["VOICE_IDS", "pickRandomVoice"]
    - path: "lib/pokemon.ts"
      provides: "PokéAPI helper and artwork URL builder"
      exports: ["fetchFirst250", "getOfficialArtworkUrl", "Pokemon"]
  key_links:
    - from: "lib/pokemon.ts"
      to: "https://pokeapi.co/api/v2/pokemon"
      via: "fetch call"
      pattern: "pokeapi\\.co/api/v2/pokemon"
    - from: "lib/voices.ts"
      to: "ElevenLabs API"
      via: "voice ID constants"
      pattern: "Gsndh0O5AnuI2Hj3YUlA"
---

<objective>
Create the lib layer: scenes config, voices config, and Pokemon API helper. These are the shared utilities consumed by API routes and components.

Purpose: Define the data contracts and helpers that all API routes and components depend on.
Output: Three lib files with typed exports.
</objective>

<execution_context>
@/Users/lumos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lumos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-full-app-build/01-CONTEXT.md
@.planning/phases/01-full-app-build/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/scenes.ts with scene configuration</name>
  <files>lib/scenes.ts</files>
  <read_first>
    - /Users/lumos/Projects/pjs-pokemon/.planning/phases/01-full-app-build/01-CONTEXT.md
  </read_first>
  <action>
Create `lib/scenes.ts` with the following exact content:

```typescript
export interface Scene {
  id: string
  name: string
  file: string
  thumbnail: string
}

export const scenes: Scene[] = [
  { id: 'canyon', name: 'Canyon', file: 'canyon.jpg', thumbnail: '/images/canyon.jpg' },
  { id: 'coast', name: 'Coast', file: 'coast.jpg', thumbnail: '/images/coast.jpg' },
  { id: 'forest', name: 'Forest', file: 'forest.jpg', thumbnail: '/images/forest.jpg' },
  { id: 'glacier', name: 'Glacier', file: 'glacier.jpg', thumbnail: '/images/glacier.jpg' },
]

export const MUSIC_TRACKS = [
  'pokemon-theme.mp3',
  'title-screen.mp3',
  'jigglypuffs-song.mp3',
  'opening-theme.mp3',
  'professor-oaks-theme.mp3',
  'bicycle-theme.mp3',
  'lance-and-red-battle-theme.mp3',
  'celadon-city-fuchsia-city.mp3',
  'pokemon-center-lofi.mp3',
  'route-3-lofi.mp3',
]

export function pickRandomTrack(): string {
  return MUSIC_TRACKS[Math.floor(Math.random() * MUSIC_TRACKS.length)]
}
```

The `file` field is the filename in public/images/ (used by the composite API route with path.join).
The `thumbnail` field is the URL path for `<img src>` in the SceneSelector component.
MUSIC_TRACKS is the complete list of 10 renamed music files for the MusicPlayer component.
  </action>
  <verify>
    <automated>cd /Users/lumos/Projects/pjs-pokemon && test -f lib/scenes.ts && grep -q "export interface Scene" lib/scenes.ts && grep -q "canyon" lib/scenes.ts && grep -q "coast" lib/scenes.ts && grep -q "forest" lib/scenes.ts && grep -q "glacier" lib/scenes.ts && grep -q "MUSIC_TRACKS" lib/scenes.ts && grep -q "pickRandomTrack" lib/scenes.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - lib/scenes.ts exports `Scene` interface with id, name, file, thumbnail fields
    - lib/scenes.ts exports `scenes` array with exactly 4 entries: canyon, coast, forest, glacier
    - lib/scenes.ts exports `MUSIC_TRACKS` array with exactly 10 slug-safe mp3 filenames
    - lib/scenes.ts exports `pickRandomTrack` function
    - All thumbnail paths start with "/images/"
  </acceptance_criteria>
  <done>lib/scenes.ts created with Scene type, 4 scenes, 10 music tracks, and pickRandomTrack helper.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/voices.ts with ElevenLabs voice config</name>
  <files>lib/voices.ts</files>
  <read_first>
    - /Users/lumos/Projects/pjs-pokemon/.planning/phases/01-full-app-build/01-CONTEXT.md
  </read_first>
  <action>
Create `lib/voices.ts` with this exact content:

```typescript
export const VOICE_IDS = [
  'Gsndh0O5AnuI2Hj3YUlA',
  'O4fnkotIypvedJqBp4yb',
  'iukn3a1vSSNFmdi5NZS4',
] as const

export type VoiceId = typeof VOICE_IDS[number]

export function pickRandomVoice(): VoiceId {
  return VOICE_IDS[Math.floor(Math.random() * VOICE_IDS.length)]
}
```

These are the exact 3 ElevenLabs voice IDs from the user spec. One is randomly selected per TTS generation.
  </action>
  <verify>
    <automated>cd /Users/lumos/Projects/pjs-pokemon && test -f lib/voices.ts && grep -q "Gsndh0O5AnuI2Hj3YUlA" lib/voices.ts && grep -q "O4fnkotIypvedJqBp4yb" lib/voices.ts && grep -q "iukn3a1vSSNFmdi5NZS4" lib/voices.ts && grep -q "pickRandomVoice" lib/voices.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - lib/voices.ts exports VOICE_IDS array with exactly 3 IDs: Gsndh0O5AnuI2Hj3YUlA, O4fnkotIypvedJqBp4yb, iukn3a1vSSNFmdi5NZS4
    - lib/voices.ts exports pickRandomVoice function returning one of the 3 IDs
    - lib/voices.ts exports VoiceId type
  </acceptance_criteria>
  <done>lib/voices.ts created with 3 ElevenLabs voice IDs and random picker function.</done>
</task>

<task type="auto">
  <name>Task 3: Create lib/pokemon.ts with PokéAPI helper</name>
  <files>lib/pokemon.ts</files>
  <read_first>
    - /Users/lumos/Projects/pjs-pokemon/.planning/phases/01-full-app-build/01-RESEARCH.md
  </read_first>
  <action>
Create `lib/pokemon.ts` with this exact content:

```typescript
export interface Pokemon {
  id: number
  name: string
}

let pokemonCache: Pokemon[] | null = null

export async function fetchFirst250(): Promise<Pokemon[]> {
  if (pokemonCache) return pokemonCache

  const res = await fetch('https://pokeapi.co/api/v2/pokemon?limit=250&offset=0')
  if (!res.ok) throw new Error(`PokéAPI error: ${res.status}`)

  const data = await res.json()
  pokemonCache = data.results.map((p: { name: string; url: string }) => {
    // Extract ID from URL: https://pokeapi.co/api/v2/pokemon/25/ -> 25
    const segments = p.url.replace(/\/$/, '').split('/')
    const id = parseInt(segments[segments.length - 1], 10)
    return { id, name: p.name }
  })

  return pokemonCache!
}

export function getOfficialArtworkUrl(id: number): string {
  return `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${id}.png`
}
```

Key details:
- The cache is module-scope, so subsequent calls return instantly without hitting PokéAPI again.
- Pokemon ID is extracted from the PokéAPI URL (not from array index) to handle any ordering.
- The artwork URL uses the GitHub sprites CDN with the `official-artwork` path (hyphen, not underscore).
  </action>
  <verify>
    <automated>cd /Users/lumos/Projects/pjs-pokemon && test -f lib/pokemon.ts && grep -q "fetchFirst250" lib/pokemon.ts && grep -q "getOfficialArtworkUrl" lib/pokemon.ts && grep -q "pokeapi.co/api/v2/pokemon" lib/pokemon.ts && grep -q "official-artwork" lib/pokemon.ts && grep -q "pokemonCache" lib/pokemon.ts && grep -q "export interface Pokemon" lib/pokemon.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - lib/pokemon.ts exports Pokemon interface with id (number) and name (string)
    - lib/pokemon.ts exports fetchFirst250 that calls https://pokeapi.co/api/v2/pokemon?limit=250&offset=0
    - lib/pokemon.ts uses module-scope pokemonCache variable for caching
    - lib/pokemon.ts exports getOfficialArtworkUrl that returns GitHub sprites CDN URL with "official-artwork" (hyphen)
    - Pokemon ID is extracted from URL, not from array index
  </acceptance_criteria>
  <done>lib/pokemon.ts created with fetchFirst250 (cached), getOfficialArtworkUrl, and Pokemon type.</done>
</task>

</tasks>

<verification>
- All 3 lib files exist and export their types/functions
- TypeScript compiles without errors: `npx tsc --noEmit lib/scenes.ts lib/voices.ts lib/pokemon.ts` (or next build picks them up)
</verification>

<success_criteria>
- lib/scenes.ts exports Scene type, scenes array (4 entries), MUSIC_TRACKS (10 entries), pickRandomTrack
- lib/voices.ts exports VOICE_IDS (3 entries), VoiceId type, pickRandomVoice
- lib/pokemon.ts exports Pokemon type, fetchFirst250 (with cache), getOfficialArtworkUrl
</success_criteria>

<output>
After completion, create `.planning/phases/01-full-app-build/01-02-SUMMARY.md`
</output>
